# DeepSeek-PDF-to-Markdown (DPM) 项目需求文档（更新版）

## 一、项目概述

- **项目名称**：DeepSeek-PDF-to-Markdown（DPM）
- **项目目标**：提供一个前后端分离的 Web 应用，允许用户上传 PDF，调用 DeepSeek-OCR 远程 API 识别内容，并输出结构化 Markdown 文件。
- **核心价值**：
  - 高精度远程 OCR 能力
  - 零成本自部署的开源方案
  - 支持匿名或注册用户的限次免费体验
  - 容器化部署，便于扩展与维护

## 二、技术架构

### 2.1 技术选型

| 模块 | 技术栈 | 版本要求 | 说明 |
|------|--------|----------|------|
| 前端 | Next.js + Tailwind CSS | Latest | 文件上传、状态展示与 Markdown 预览 |
| 后端 | FastAPI (Python) | >=3.10 | 提供 API 服务，负责 PDF 处理与 OCR 调用 |
| OCR 服务 | DeepSeek-OCR 远程 API | 最新 | 通过 SiliconCloud 等官方远程接口调用 |
| 缓存/限流 | Redis | >=7.0 | 存储 Token、任务状态与限流信息 |
| 文件处理 | pdf2image + Pillow | Latest | 将 PDF 分页为图片，供 OCR 使用 |
| 容器化 | Docker + Docker Compose | Latest | 前后端与依赖服务一键部署 |

### 2.2 系统架构

```
用户浏览器
    │
    ▼
Frontend (Next.js)
    │  上传 PDF / 获取 Token
    ▼
Backend (FastAPI)
 ├── /api/token/create    ← Token 生成接口
 ├── /api/convert         ← OCR 转换接口
 ├── /api/status/{id}     ← 任务状态查询
 │
 ├── DeepSeek-OCR 远程 API 客户端封装
 ├── Redis 限流与状态模块
 ├── PDF 拆页 + OCR + Markdown 合成
 │
 └── 本地文件存储（上传、输出）
```

## 三、功能需求

### 3.1 前端
- ✅ PDF 文件上传（支持拖拽、大小与格式校验）
- ✅ Token 生成与验证交互
- ✅ Markdown 预览、下载与复制
- ✅ 任务状态实时显示（轮询/状态组件）
- ✅ 错误提示与用户指引

### 3.2 后端
- ✅ Token 生成与管理（Redis 存储，限次数与有效期）
- ✅ PDF 上传与校验（仅 PDF，≤100 MB）
- ✅ PDF 拆页处理（pdf2image + Pillow）
- ✅ DeepSeek-OCR 远程 API 封装调用
- ✅ Markdown 生成与格式化（尽量还原 PDF 排版）
- ✅ 限次数与基础安全校验
- ✅ 本地文件存储与 24 小时清理机制
- ✅ 简易日志记录（INFO/ERROR 级别）

### 3.3 基础设施
- ✅ Docker 容器化与 Compose 编排
- ✅ Redis 服务配置
- ✅ 环境变量与密钥管理（API Key 等）
- ✅ 部署与开发文档

## 四、非功能需求

### 4.1 性能与限制

| 项目 | 数值 | 说明 |
|------|------|------|
| 单文件大小 | ≤100 MB | 防止内存溢出 |
| 并发处理 | 5（初始） | 暂不引入任务队列，由 FastAPI 直接处理 |
| 单页转换延迟 | 1~2 秒（GPU） | 依赖远程 API 响应 |
| Token 使用次数 | 5 次/小时 | 默认配置，可调 |
| Token 有效期 | 1 小时 | 防止滥用 |
| 临时文件保留 | 24 小时 | 定期清理脚本 |

### 4.2 安全要求
1. ✅ 文件类型与 MIME 检测
2. ✅ 文件大小限制
3. ✅ 随机文件名与目录隔离
4. ✅ Token 验证必需
5. ✅ 基于 Redis 的使用频次控制
6. ✅ HTTPS 支持（部署阶段配置）
7. ✅ 日志脱敏，避免泄露 Token 与 API Key

### 4.3 日志与监控
- 使用统一的 `logger` 模块，输出到控制台/文件。
- 记录关键流程：上传、拆页、OCR 调用、Markdown 合成、文件写入与清理。
- 异常需记录堆栈，敏感信息需脱敏。

## 五、业务流程

1. 用户获取 Token（匿名或注册用户都遵守限次规则）。
2. 上传 PDF 文件，后端校验并拆页。
3. 每页图片调用 DeepSeek-OCR 远程 API，获取文本与版面信息。
4. 根据位置信息与内容生成尽量还原的 Markdown：
   - 识别标题、段落、列表、表格、图片等结构。
   - 保持页面间的逻辑顺序与分隔。
5. 将最终 Markdown 存储在本地输出目录，并返回下载链接。
6. 定期清理超时的临时文件与过期结果。

## 六、开发阶段规划

1. **阶段 1：后端原型**
   - FastAPI 框架、配置管理、API 路由与数据模型
   - 深入远程 OCR API 封装与异常处理
   - PDF 拆页与 Markdown 合成流程

2. **阶段 2：前端界面**
   - 文件上传、Token 交互、进度展示与结果预览
   - 与后端接口联调

3. **阶段 3：限流与安全**
   - Redis 集成与 Token/频次校验
   - 简易 IP 限制与错误处理

4. **阶段 4：部署与优化**
   - Docker、docker-compose、环境变量管理
   - 日志与监控基础设施

5. **阶段 5：试运行与反馈**
   - 演示环境搭建
   - 收集反馈与持续优化

## 七、当前工作重点

- ✅ 确认远程 DeepSeek-OCR API 接入策略与配置项
- ✅ 明确 Markdown 需要尽量还原 PDF 格式
- ✅ 确认所有文件存储于本地并维持清理机制
- ✅ 暂不引入任务队列，保持 FastAPI 同步处理
- ✅ 维持基础的 INFO/ERROR 日志记录体系

## 八、待办事项概览

| 模块 | 状态 | 说明 |
|------|------|------|
| 远程 OCR API 封装 | ⏳ 进行中 | 处理鉴权、重试与错误回退 |
| Markdown 结构化后处理 | ⏳ 进行中 | 需设计结构识别与格式化策略 |
| 本地存储清理脚本 | ⏳ 进行中 | 定期删除过期文件，保留 24h |
| 日志完善 | ⏳ 进行中 | 核心节点日志、脱敏处理 |
| 前后端联调 | ⏳ 进行中 | 验证端到端流程 |

## 九、变更记录

| 日期 | 变更内容 | 说明 |
|------|-----------|------|
| 2025-01-XX | 初版需求文档 | 定义整体规划与功能 | 
| 2025-01-XX | 更新远程 OCR 与 Markdown 需求 | 明确远程 API、排版还原、本地存储、无队列与简易日志策略 |

